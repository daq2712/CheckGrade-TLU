# CheckGrade-TLU
Code sử dụng để xem điểm thi trên web `sinhvien1.tlu.edu.vn`, có thể thành công kể cả khi không thể truy cập được web bằng browser bình thường do có quá nhiều người cùng truy cập cùng một lúc.

Requirements:

```
pip install httpx
```
